# Simple placeholder Dockerfile to test dependencies
FROM node:20-slim

WORKDIR /app

# A long-running dummy command to keep the container alive
CMD ["node", "-e", "console.log('Marka AI Web Started Successfully. Waiting for React implementation...'); setInterval(() => {}, 1000)"]