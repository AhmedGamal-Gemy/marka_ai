# Simple placeholder Dockerfile to test dependencies
FROM python:3.12-slim

WORKDIR /app

# A long-running dummy command to keep the container alive
CMD ["python", "-c", "import time; print('Marka AI Bot Started Successfully. Waiting for implementation...'); [time.sleep(10) for _ in range(1000)]"]